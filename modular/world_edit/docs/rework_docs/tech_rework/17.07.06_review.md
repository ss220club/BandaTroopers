# Вердикт по `cc210d870f8fa774de38c59b83de86f09c933ff2`

Текущий head PR действительно соответствует указанному коммиту. PR остаётся draft.

Результат **не принимается**. Генерация стала структурнее, но архитектурно всё ещё выглядит как:

```text
несколько прямоугольников
→ связанные произвольными проходами
→ остаток площади объявлен open bay / circulation
→ в комнаты положена минимальная композиция, достаточная для валидатора
```

На первом изображении центральная свободная область доминирует над функциональными помещениями, периферийные комнаты повторяются и слабо наполнены. На втором — чрезмерно крупные помещения, плавающая столовая группа в пустом зале, отдельный блок кроватей и множество незадействованных квадратных метров.

Это уже не хаотический scatter. Это **предсказуемый дефект текущей архитектуры solver-а**.

## Предпочтительное решение

**94% — переработать текущий canonical solver изнутри.**

Не создавать:

* `v3`;
* `next`;
* `experimental`;
* новый runtime-switch;
* альтернативный fallback pipeline.

Оставить один entrypoint, но удалить маскирующую логику и заменить псевдопаттерны реальными topology constraints.

**6% — продолжать менять score weights, density thresholds и размеры комнат.** Этот вариант дешевле, но проблему не решит: текущие topology families пока не являются разными алгоритмами построения.

---

# 1. Почему текущие topology families фактически не работают

В коде уже объявлены:

```text
hub_spoke
split_wing
open_bay_perimeter
secure_core
nested_service
compound_cells
axial_fallback
```

Но все они являются пустыми subtype с разным `id`. Общий `build_region_candidates()` для каждого вызывает один и тот же `build_building_layout_topology_region_candidate()`.

Сам generic builder для **каждой** family создаёт одну и ту же influence zone:

```dm
region.add_influence_zone(
    "functional_field",
    "functional",
    2,
    2,
    width - 1,
    height - 1,
    functional_ids,
    100,
)
```

То есть вся внутренняя площадь является единым прямоугольным полем размещения. Семейство влияет в основном на мягкий score, а не на геометрические ограничения.

В результате `hub_spoke` не требует hub со spokes, `split_wing` не создаёт два крыла, `nested_service` не обеспечивает вложенность, а `open_bay_perimeter` не определяет границы bay и service perimeter.

## Что заменить

Нужен один canonical interface:

```dm
/datum/world_edit_building_layout_family_policy
    var/id = ""
    var/root_policy = ""
    var/route_policy = ""
    var/max_branch_depth = 0
    var/max_root_children = 0
    var/min_open_bay_ratio = 0
    var/max_open_bay_ratio = 0
    var/max_route_ratio = 0

    proc/build_constraints(
        datum/world_edit_building_layout_context/context,
        datum/world_edit_building_layout_topology_graph/graph
    )

    proc/build_seed_regions(
        datum/world_edit_building_layout_context/context,
        datum/world_edit_building_layout_topology_graph/graph,
        orientation_variant
    )

    proc/score_partial_candidate(...)
    proc/validate_candidate(...)
```

Это не новая версия solver-а. Это замена текущих пустых subtype внутри существующего solver.

---

# 2. Удалить логику, которая маскирует topology defects

## 2.1 Удалить автоматическое подключение всех disconnected nodes к root

Сейчас `connect_building_layout_topology_components()` при обнаружении disconnected node просто создаёт:

```dm
root → node
kind = EDGE_ROUTE
```

Так semantic graph никогда по-настоящему не бывает ошибочным: неполный граф автоматически превращается в star topology.

### Новое правило

```text
required disconnected component → contract compilation failure
optional disconnected node       → исключить или подключить по явной optional policy
circulation transition           → отдельный route terminal, не скрытый root edge
```

То есть функцию:

```dm
connect_building_layout_topology_components()
```

нужно удалить.

Вместо неё:

```dm
validate_building_layout_topology_graph_contract()
```

которая возвращает ошибки:

```text
topology.required_component_disconnected
topology.required_node_without_parent
topology.circulation_transition_unresolved
topology.nested_parent_missing
```

---

## 2.2 Не терять adjacency, связанный с circulation zones

`compile_building_layout_connection_contracts()` получает IDs только через `functional_room_contracts`. Если одна сторона adjacency — `entry`, `route` или `choke`, список оказывается пустым, и правило пропускается.

Это ломает цепочки вроде:

```text
entry_buffer
→ central_aisle
→ ready_area
→ sleep_bay
```

или:

```text
dining_front
→ serving_edge
→ prep_core
→ cooking_line
```

Такие связи присутствуют в archetype data, но circulation endpoints не становятся полноценными topology terminals.

### Нужно компилировать три типа узлов

```dm
#define LAYOUT_NODE_FUNCTIONAL "functional"
#define LAYOUT_NODE_CIRCULATION_TERMINAL "circulation_terminal"
#define LAYOUT_NODE_TRANSITION "transition"
```

Circulation node не считается комнатой, но участвует в graph:

```text
entry terminal
route junction
choke / serving threshold
functional room
```

---

## 2.3 Удалить универсальный `room → route` pass

После allocation текущий код добавляет route connection для **каждой** комнаты независимо от topology graph.

Этот цикл должен быть удалён:

```dm
for(room_plan in candidate.room_plans)
    add_connection(room_plan.id, "route")
```

Должны материализоваться только edges, объявленные topology graph:

```text
room ↔ room
room ↔ open bay
room ↔ route terminal
nested room ↔ parent
secure room ↔ controlled threshold
```

---

# 3. Удалить blanket circulation overlay

Это наиболее вероятная непосредственная причина огромной центральной пустоты на первом изображении.

Сейчас `materialize_building_layout_circulation_overlay()` берёт **каждую** внутреннюю клетку, которая:

* не принадлежит комнате;
* не является стеной reserve;
* не входит в route;

и объявляет её:

```dm
route_overlay_turfs
zone = "circulation_open_bay"
```

После этого circulation contracts получают произвольные клетки из общего списка по порядку, а не связные пространственные регионы.

## Эта функция должна быть удалена

```dm
materialize_building_layout_circulation_overlay()
```

## Взамен нужны только два явных вида circulation

### Enclosed route

Самостоятельный связный floor component:

```text
main corridor
service branch
secure approach
entry vestibule
```

### Room-owned aisle overlay

Принадлежит конкретному functional/open-bay room:

```text
storage aisle
hydro service aisle
workshop work lane
dormitory central aisle
```

```dm
/datum/world_edit_building_layout_route_overlay
    var/id
    var/owner_room_id
    var/kind
    var/list/turfs
    var/min_width
    var/required
```

Неиспользованный остаток площади не должен автоматически становиться circulation.

Он должен:

1. расширить подходящую комнату;
2. стать явным structural buffer;
3. либо привести к reject кандидата.

---

# 4. Полностью заменить текущий route solver

Сейчас `solve_building_layout_route_after_rooms()` фактически выбирает только один entry seed turf и возвращает `TRUE`. Затем каждая комната независимо протягивает connector до уже существующего route.

Это не route network solver.

## Целевая модель

```text
entry terminal
+ topology route terminals
+ room access ports
→ bounded minimal network
```

### Алгоритм

1. Собрать terminals:

   * вход;
   * edges типа `ROUTE`;
   * controlled-room access ports;
   * public transition ports.

2. Сохранить прямые `room ↔ room` edges, если есть shared wall.

3. Соединять только terminals, которым нужен corridor.

4. Использовать incremental bounded A*:

   * каждый новый terminal соединяется с ближайшим существующим route component;
   * уже построенный route имеет нулевую или малую дополнительную стоимость;
   * room tiles, partition reserves и required wall service bands запрещены.

5. После каждого соединения упрощать маршрут:

   * удалить петли без функциональной причины;
   * удалить лишние dead ends;
   * сократить параллельные ветви;
   * сгладить staircase, если возможен прямой orthogonal segment.

## Стоимости A*

```text
existing route                 0
free interior                  10
near public room               8
near service room             12
turn                          +12
dead-end extension            +20
adjacent parallel route       +25
through room                  forbidden
through reserved partition    forbidden
through service frontage      forbidden
```

---

# 5. Реальные правила topology families

## 5.1 `hub_spoke`

Применение:

```text
living
office
medbay
chapel
```

Жёсткие правила:

* root room располагается около геометрического центра, но со смещением к входу;
* максимум 3–4 прямых child spaces;
* минимум две разные стороны root используются детьми;
* public child может иметь open merge;
* private/service child получает controlled door;
* corridor создаётся только для второго уровня graph.

Недопустимо:

```text
все комнаты цепочкой
все комнаты на одной стороне
огромный безымянный central residual
```

---

## 5.2 `split_wing`

Применение:

```text
living
dormitory
laboratory
kitchen
```

Правила:

* graph разбивается на два branch-subgraph;
* root/public transition располагается между крыльями;
* private/clean/secure roles группируются по wing;
* ветви не должны пересекаться;
* минимум одна functional room в каждом wing;
* rooms одной ветви размещаются рядом друг с другом, а не независимо рядом с route.

---

## 5.3 `open_bay_perimeter`

Применение:

```text
workshop
storage
hydroponics
dormitory
```

Правила:

* один **именованный** open-bay room занимает 35–60% useful interior;
* bay принадлежит semantic zone, а не `circulation_open_bay`;
* service/private rooms размещаются по его периметру;
* внутри bay создаётся owner-owned aisle;
* мебели разрешено занимать bay только в заранее определённых bands;
* aisle не оборачивается стенами.

Центр на первом скриншоте допустим только если это, например:

```text
grow_rows
work_bay
storage_floor
sleep_bay
```

с полноценной внутренней композицией, а не остаток allocation.

---

## 5.4 `secure_core`

Применение:

```text
security
checkpoint
laboratory
```

Правила:

```text
entry/public buffer
→ controlled threshold
→ secure core
→ nested service/storage
```

* secure core не граничит непосредственно с external entry;
* один controlled choke;
* public и secure paths не смешиваются;
* storage/security machinery располагаются за threshold.

---

## 5.5 `nested_service`

Применение:

```text
kitchen
medbay
laboratory
office
```

Правила:

* outer open/work room;
* inner room полностью находится в outer bounds либо делит с ним длинную стену;
* inner margin не менее одной клетки;
* nested room имеет один controlled opening;
* nested room не получает независимую ветвь общего коридора.

---

## 5.6 `compound_cells`

Применение:

```text
compound_colony
engineering
large security
```

Правила:

* несколько room pods;
* explicit courtyard/yard;
* yard не считается незанятым остатком;
* pods связаны короткими branches;
* family недоступна для compact footprint.

---

## 5.7 `axial_fallback`

Оставить только как fallback:

```dm
can_solve =
    compact
    || min(width, height) <= 11
    || max(width, height) / min(width, height) >= 1.7
```

Для standard/spacious rectangular footprint axial должен иметь сильный penalty и использоваться только при отсутствии других hard-valid family.

---

# 6. Переработать room allocator

Сейчас rooms размещаются greedily по одному из одного общего `functional_field`. Family различия выражены soft score.

## 6.1 Убрать single-field allocation

Family должна создавать разные placement regions:

```text
root_region
left_wing
right_wing
perimeter_north
perimeter_south
secure_core_region
nested_parent_region
```

Не:

```text
functional_field = весь интерьер
```

---

## 6.2 Использовать bounded beam search

```dm
#define LAYOUT_ALLOCATION_BEAM_WIDTH 6
#define LAYOUT_RECTS_PER_NODE 8
#define LAYOUT_MAX_PARTIAL_EXPANSIONS 96
```

Псевдокод:

```dm
var/list/beam = list(initial_partial_candidate)

for(var/topology_node in ordered_nodes)
    var/list/next_beam = list()

    for(var/partial in beam)
        var/list/rect_candidates = build_room_rect_candidates(
            partial,
            topology_node,
            LAYOUT_RECTS_PER_NODE
        )

        for(var/rect in rect_candidates)
            var/new_partial = partial.clone()
            new_partial.place_room(topology_node, rect)

            if(validate_partial_layout(new_partial))
                next_beam += new_partial

    beam = select_top_partial_candidates(
        next_beam,
        LAYOUT_ALLOCATION_BEAM_WIDTH
    )
```

Это остаётся bounded и детерминированным.

---

## 6.3 Geometry должна удовлетворять edge, а не только получать бонус

Для каждого edge:

| Edge         | Геометрическое условие                                 |
| ------------ | ------------------------------------------------------ |
| `SHARED`     | общий wall run не короче `min_shared_wall`             |
| `OPEN_MERGE` | общий сегмент 2–4 клетки, без partition                |
| `SECURE`     | общий segment с controlled opening                     |
| `NESTED`     | child внутри parent либо внутри declared nested region |
| `ROUTE`      | доступный access terminal для route network            |

Сейчас graph parent влияет в основном на расстояние между центрами комнат. Этого недостаточно.

---

## 6.4 Удалить мёртвый direct-route fast path

В `find_best_building_layout_room_rect_for_contract()` всё ещё есть ветка:

```dm
if(length(candidate.route_turfs))
    find_building_layout_direct_route_room_rect(...)
```

Но current pipeline размещает rooms до полноценного route. Эта ветка либо мертва, либо относится к старой архитектуре.

После `rg` callsite audit удалить:

```dm
find_building_layout_direct_route_room_rect()
building_layout_rect_min_route_distance()
```

если они больше не используются каноническим path.

---

## 6.5 Удалить ad-hoc `score_building_layout_parallel_room_spacing`

Для `hub_spoke` применяется штраф до `5000 × overlap`, чтобы искусственно раздвигать помещения. Это лечит конкретный симптом, а не реализует topology.

Заменить hard constraints:

```text
required shared edge
forbidden accidental corridor slot
minimum partition gap
maximum root-child distance
side occupancy rule
```

---

# 7. Исправить размеры комнат

На больших footprint сейчас используется:

```dm
preferred_fill_ratio = 1.0
preferred_area = average_room_area
```

То есть rooms пытаются распределить почти всю полезную площадь между functional rooms.

Отсюда второй скриншот: несколько слишком крупных помещений с малым количеством мебели.

## Новая area policy

### Enclosed rooms

```text
preferred_area = max(scene_min_area, authored preferred area)
max_area       = preferred_area × 1.25–1.40
```

### Open bay

```text
может поглощать surplus
но только до family max_open_bay_ratio
```

### Circulation

```text
вычисляется route solver
не участвует в average room area
```

### Structural slack

```text
3–8% interior в зависимости от footprint
```

## Room area должна выводиться из module footprint

```text
required occupied cells
+ interaction/front clearance
+ door-to-focus lane
+ wall service band
+ negative-space reserve
```

Например, dining table с четырьмя стульями — не просто `5` объектов:

```text
5 occupied tiles
+ 4 interaction tiles
+ route approach
≈ минимум 12–16 useful room tiles
```

---

# 8. Удалить маскирующий scene-код

## 8.1 Удалить synthetic composition support

Сейчас `build_building_layout_scene_instance_support_spec()` автоматически создаёт:

* chair;
* sink;
* crate;
* cabinet;

чтобы добить минимальное число members.

Это создаёт формально заполненную, но бессмысленную комнату.

Удалить функцию полностью.

Комната должна удовлетворять authored composition, а не абстрактному количеству предметов.

---

## 8.2 Удалить `reserve_building_layout_required_scene_identity_budget`

Функция повышает global category budgets под уже скомпилированные fallback/support requirements.

Это инвертированная модель:

```text
не бюджет ограничивает программу,
а программа автоматически увеличивает бюджет до успеха
```

Заменить preflight:

```text
required composition demand
≤ available provider/category budget
```

Иначе:

```text
program.required_composition_budget_unreachable
```

---

## 8.3 Удалить required singleton fallback

Сейчас role fallback может превратить комнату в:

```text
private → bed/cabinet
storage → rack
hub → console
entry → light
default → table
```

Required room не должна считаться готовой из-за одного generic объекта.

```dm
/datum/world_edit_building_layout_scene_contract
    var/allow_required_fallback = FALSE
    var/allow_optional_singleton_fallback = FALSE
```

Правило:

```text
required scene:
    curated module
    либо explicit compact substitute
    иначе candidate reject

optional detail:
    singleton допустим только при explicit allow
```

---

## 8.4 Ставить все required composition groups

Сейчас solver выбирает только один `best_required` cluster spec — с наибольшим priority. Остальные required specs не становятся обязательными группами.

Это нужно заменить:

```dm
for(var/required_group in scene_contract.required_composition_groups)
    if(!solve_required_group(required_group))
        return FALSE
```

Например kitchen должна требовать независимо:

```text
serving counter
prep surface
cooking line
cold storage
```

а не один наиболее приоритетный cluster.

---

# 9. Placement modules должны быть атомарными до конца pipeline

Curated module placement уже умеет создать atomic candidate и записывает module instance metadata. Это хорошая основа.

Но атомарность ломается позже.

## 9.1 Не удалять members по одному из-за budget

`allocate_building_layout_optional_scene_budget()` проходит по отдельным members и выкидывает non-major objects по category budget.

Так можно получить:

```text
стол без стула
часть rack run
одну машину из группы
```

Нужно бюджетировать module instance целиком:

```dm
scene_budget.can_use_module(module_candidate)
scene_budget.commit_module(module_candidate)
```

Если budget не позволяет весь module — module не ставится.

---

## 9.2 Negative space резервировать до module solve

Сейчас negative-space path строится после расстановки, а конфликтующие optional members удаляются по одному.

Правильный порядок:

```text
door/access points
→ выбрать focus zone
→ зарезервировать door-to-focus path
→ зарезервировать interaction lanes
→ решать placement modules
```

Никакого удаления членов готового модуля после placement.

---

## 9.3 Wall-run module обязан иметь единую wall axis

Сейчас wall-required module проверяет лишь наличие любой adjacent wall у каждого member, а затем выбирает первый найденный `wall_dir`.

Нужно:

```text
все wall-required members:
    один wall_dir
    один contiguous wall segment
    одинаковый facing contract
    свободный service/front aisle
```

## 9.4 Clearance проверять и для wall modules

Сейчас authored `clearance_offsets` проверяются только для `!module.wall_required`.

Это неверно. Стеллажи, машины, шкафы и кровати тоже требуют фронтального доступа.

Разделить masks:

```dm
occupied_offsets
front_access_offsets
interaction_offsets
aisle_offsets
forbidden_offsets
```

---

# 10. Role-specific composition contracts

Одного `occupied_ratio` недостаточно. Текущие generic thresholds — 8–14% в зависимости от role — могут пропустить неудачную композицию, если объекты просто занимают достаточное число клеток.

## Новый datum

```dm
/datum/world_edit_building_layout_composition_contract
    var/id = ""
    var/list/required_module_groups = list()
    var/min_module_instances = 0
    var/list/min_capacity_by_kind = list()
    var/min_occupied_ratio = 0
    var/max_occupied_ratio = 50
    var/repeat_policy = "per_instance"
```

## Обязательные правила

| Пространство           | Composition                                                    |
| ---------------------- | -------------------------------------------------------------- |
| Dining                 | один table module + минимум 2 chairs того же instance          |
| Living common          | focal social module + secondary wall/side module при area ≥24  |
| Dorm sleep bay         | минимум 2 beds на каждый repeated bay; clear access у каждой   |
| Locker strip           | минимум 2 cabinets в authored wall module                      |
| Workshop               | workbench/machine run + support storage                        |
| Workshop storage       | rack module; area ≥12 — минимум 2 rack members                 |
| Laboratory machine bay | machine group, не более двух машин в одном contiguous wall run |
| Laboratory analysis    | work surface + seat/console + storage/support                  |
| Kitchen serving        | counter run минимум 3 cells                                    |
| Kitchen cooking        | cooking line минимум 4 functional members                      |
| Kitchen cold storage   | минимум 2 cold-storage members                                 |
| Storage                | rack rows + проход между рядами                                |
| Hydro                  | tray rows/grid + service aisle                                 |
| Sanitation             | toilet + sink, если room contract не compact                   |
| Bedroom                | bed + clear approach; storage при area ≥16                     |

---

# 11. Repeated room instances: явная политика

Сейчас repeated instances получают клонированные и мутированные cluster specs, включая автоматическую замену на compact и изменение количества.

Вместо этого в cluster spec:

```dm
#define CLUSTER_INSTANCE_GLOBAL_ONCE "global_once"
#define CLUSTER_INSTANCE_PRIMARY_ONLY "primary_only"
#define CLUSTER_INSTANCE_PER_INSTANCE "per_instance"
#define CLUSTER_INSTANCE_DISTRIBUTE_TOTAL "distribute_total"

var/instance_policy = CLUSTER_INSTANCE_PRIMARY_ONLY
var/min_per_instance = 0
var/max_per_instance = 999
var/capacity_credit = 1
```

Примеры:

```text
public focal dining        GLOBAL_ONCE
dorm beds                  DISTRIBUTE_TOTAL, min_per_instance=2
locker wall                PER_INSTANCE
workshop service wall      PER_INSTANCE
storage rack aisle         PER_INSTANCE
lab machines               DISTRIBUTE_TOTAL
infrastructure             separate global pass
```

Нельзя автоматически превращать wall table в table cluster или добавлять случайный cabinet только потому, что это instance `_2`.

---

# 12. Wall generation по правилам маппинга

## 12.1 Partition graph — единственный источник внутренних стен

Для каждой пары соседних floor ownership cells:

```text
один owner                       → стены нет
OPEN_MERGE edge                  → стены нет
SOFT partition                   → стена, кроме arch
CLOSED partition                 → стена, кроме door
SECURE partition                 → secure wall/door
room-owned aisle overlay         → стены нет
external boundary                → exterior wall/window/exit
```

Не:

```text
прямоугольный perimeter каждой комнаты
→ массовый cleanup
```

---

## 12.2 Базовые mapping rules

| Правило                           |                                          Gate |
| --------------------------------- | --------------------------------------------: |
| Толщина внутренней стены          |                                  ровно 1 tile |
| Длина partition segment           |            ≥3 tiles, service nook допускает 2 |
| Door distance from corner         |                          ≥1 цельный wall tile |
| Public arch                       |                                     2–3 tiles |
| Controlled door                   |                                        1 tile |
| Одноклеточный wall stub           |                                      запрещён |
| Одноклеточная выемка/notch        |                                     запрещена |
| Stair-step partition              | запрещена, кроме irregular footprint boundary |
| Continuous corridor canyon        |                             не более 3 slices |
| Enclosed room min clear dimension |                   3, sanitation допускает 2×3 |
| Public/open bay min dimension     |                                           5×5 |
| Enclosed room aspect              |                                          ≤2.0 |
| Service/storage aspect            |                                          ≤2.5 |
| Main route width                  |                    2 только при необходимости |
| Branch/service route              |                                        1 tile |
| RECT wall cleanup                 |                                             0 |
| Irregular cleanup ratio           |                                           ≤1% |
| Unassigned interior RECT          |                                           ≤3% |
| Unassigned irregular              |                                           ≤5% |

Конкретные проценты требуют калибровки seed matrix; уверенность в этих стартовых порогах около **80%**.

---

## 12.3 Добавить geometry defect counters

```text
layout_wall_stub_count
layout_single_tile_notch_count
layout_stair_step_partition_count
layout_misaligned_partition_join_count
layout_unassigned_interior_count
layout_unassigned_interior_ratio_percent
layout_route_area_ratio_percent
layout_open_bay_area_ratio_percent
layout_route_dead_end_count
layout_route_longest_dead_end
layout_route_turn_count
```

Текущий wall cleanup gate полезен, но он не ловит геометрически валидные, однако визуально плохие “зубы” и ломаные стены.

---

# 13. Candidate selection не должен случайно выбирать худший layout

Сейчас hard-valid family winners, находящиеся в пределах 10% от лучшего score, попадают в seeded random selection.

Это означает, что при лучшем candidate `1000` может быть выбран candidate `900`, даже если визуально он хуже.

## Заменить на lexicographic selection

```text
1. hard validity
2. zero mapping defects
3. zero composition deficits
4. zero residual/cleanup defects
5. topology quality
6. route efficiency
7. scene quality
8. seed only for exact tie
```

Seed допустим только если:

```text
final score equal
или разница <=1 point / <=0.5%
```

---

## Проверять все bounded candidates

Не прекращать trial selection после появления двух family labels.

Проверить все кандидаты в установленном cap:

```text
максимум 24 layout candidates
```

Сохранять:

```text
best hard-valid candidate per actual topology signature
```

## Signature должна отражать геометрию

```text
room adjacency matrix
route degree sequence
room graph depth distribution
open-bay count
nested edge count
room side assignment
route component shape
```

Два одинаковых layout с разными `family_id` не считаются diversity.

---

# 14. Удаление лишнего кода

Перед удалением выполнить discovery:

```bash
rg -n \
  "run_building_semantic_interiors|world_edit_building_semantic_|semantic_scene" \
  modular/world_edit/code/generators/building_layout

rg -n \
  "world_edit_building_divider_plan|world_edit_building_divider_edge_run|build_building_layout_divider" \
  modular/world_edit/code/generators/building_layout

rg -n \
  "find_building_layout_direct_route_room_rect|score_building_layout_parallel_room_spacing" \
  modular/world_edit/code/generators/building_layout

rg -n \
  "materialize_building_layout_circulation_overlay|connect_building_layout_topology_components|build_building_layout_scene_instance_support_spec|reserve_building_layout_required_scene_identity_budget" \
  modular/world_edit/code/generators/building_layout
```

## Кандидаты на удаление

| Код                                                           | Действие                                                                  |
| ------------------------------------------------------------- | ------------------------------------------------------------------------- |
| `connect_building_layout_topology_components()`               | удалить                                                                   |
| universal `[room]_to_route` loop                              | удалить                                                                   |
| `materialize_building_layout_circulation_overlay()`           | удалить                                                                   |
| current seed-only `solve_building_layout_route_after_rooms()` | заменить                                                                  |
| generic full-field topology region builder                    | заменить                                                                  |
| name-only topology subtype                                    | заменить policy implementations                                           |
| `find_building_layout_direct_route_room_rect()`               | удалить после callsite audit                                              |
| `score_building_layout_parallel_room_spacing()`               | удалить                                                                   |
| `build_building_layout_scene_instance_support_spec()`         | удалить                                                                   |
| `reserve_building_layout_required_scene_identity_budget()`    | удалить                                                                   |
| required singleton fallback                                   | удалить                                                                   |
| member-level optional budget pruning                          | удалить                                                                   |
| post-placement member deletion из negative-space pass         | удалить                                                                   |
| seeded 10% family selection                                   | удалить                                                                   |
| старый immediate-neighbor canyon validator                    | удалить                                                                   |
| legacy divider datums/helpers                                 | удалить после callsite audit                                              |
| старый `semantic/**` solver/emitter layer                     | удалить из include graph после подтверждения нулевых production callsites |

В `_world_edit.dme` одновременно подключены старые semantic solver/model/rules/emitter/validation и canonical solver. Это требует обязательного callsite audit: если canonical production path больше их не вызывает, их нужно удалить из include graph, а не сохранять как “возможный запасной путь”.

Также нужно проверить фактическое использование старых:

```text
building_layout_room_graph.dm
building_layout_bsp.dm
legacy divider datums
```

Они всё ещё присутствуют в include chain.

---

# 15. Унифицировать quality validation

Сейчас одновременно существуют:

* старый canyon detector, проверяющий непосредственных соседей route tile;

* новый route-band detector, проходящий через всю ширину route.

Оставить только route-band implementation.

## Required adjacency validator должен проверять геометрию

Сейчас review helper считает required edge выполненным, если соответствующая запись присутствует в `candidate.room_connections`. Он не проверяет, что комнаты действительно имеют shared wall или связаны корректным route transition.

Новый validator:

```dm
validate_required_topology_edge_geometry(
    candidate,
    topology_edge
)
```

Проверки:

```text
SHARED      → реальный shared wall segment
OPEN_MERGE  → реальный непрерывный merge segment
NESTED      → child containment
SECURE      → controlled opening
ROUTE       → связный route path между terminals
```

---

# 16. Обновить visualizer: показывать причину плохой генерации

В `semantic.json` на room:

```json
{
  "id": "sleep_bay_2",
  "zone_id": "sleep_bay",
  "role": "private",
  "spatial_kind": "functional_room",
  "area": 18,
  "topology_parent": "ready_area",
  "graph_depth": 2,
  "topology_edges": ["shared:ready_area"],
  "composition_id": "dorm_sleep_bay",
  "module_instance_count": 1,
  "functional_units": {
    "bed_capacity": 2
  },
  "occupied_ratio_percent": 11,
  "unassigned_adjacent_count": 0,
  "composition_satisfied": true
}
```

Overlay:

* room ownership;
* route floor;
* room-owned aisle;
* residual/unassigned;
* topology edges;
* module bounding boxes;
* interaction clearance;
* negative space;
* removed cleanup walls;
* wall stubs/notches.

Подпись:

```text
sleep_bay_2
A=18
modules=1
beds=2
density=11%
```

---

# 17. Файлы и ответственность после переработки

Разделение файлов допустимо только как декомпозиция **того же** canonical solver.

| Файл                                       | Ответственность                                |
| ------------------------------------------ | ---------------------------------------------- |
| `building_layout_solver_contracts.dm`      | datums, enums, immutable contracts             |
| `building_layout_solver_programs.dm`       | compilation semantic plan → graph/compositions |
| `building_layout_solver_topology.dm`       | family constraints и graph validation          |
| `building_layout_solver_room_allocator.dm` | bounded beam allocation                        |
| `building_layout_solver_route.dm`          | terminals, A*, route network, aisles           |
| `building_layout_solver_openings.dm`       | openings, windows, transitions                 |
| `building_layout_solver_partitions.dm`     | partition graph и direct wall model            |
| `building_layout_solver_composition.dm`    | atomic placement modules                       |
| `building_layout_solver_quality.dm`        | единый hard/soft quality contract              |
| `building_layout_solver.dm`                | orchestration и candidate selection            |

Это не `v3` и не новый entrypoint. Старый монолит можно физически разделить только после удаления дублирующей логики.

---

# 18. Порядок реализации

## Этап 0 — убрать false-green

До изменения generator-а текущие два screenshot cases должны стать красными по:

```text
layout_unassigned_interior_ratio
layout_open_bay_without_owner
layout_room_composition_missing
layout_route_network_missing
layout_single_tile_notch
layout_required_adjacency_geometry_missing
```

Нельзя сначала исправлять expectations под текущий результат.

---

## Этап 1 — удалить маскирующий код

Удалить:

```text
auto-connect topology components
universal room-to-route
blanket circulation overlay
synthetic support specs
required fallback
budget inflation
random family winner
duplicate canyon
```

После этого часть существующих cases закономерно перестанет генерироваться. Это ожидаемый переходный результат.

---

## Этап 2 — реальные family constraints

Реализовать:

```text
hub_spoke
split_wing
open_bay_perimeter
secure_core
nested_service
compound_cells
axial_fallback
```

Каждая family обязана создавать различные:

```text
placement regions
root policy
branch constraints
route policy
open-bay policy
```

---

## Этап 3 — graph-aware allocator

* bounded beam;
* hard edge geometry;
* module-aware room sizes;
* residual control;
* никакого equal-area заполнения.

---

## Этап 4 — route network и partition graph

* entry/terminal network;
* explicit owner aisles;
* direct wall materialization;
* openings по mapping rules;
* cleanup только diagnostic.

---

## Этап 5 — atomic compositions

* все required groups;
* per-instance policy;
* negative space до placement;
* module-level budgets;
* no required singleton fallback.

---

## Этап 6 — selection и acceptance

* trial всех bounded candidates;
* actual topology signatures;
* best quality wins;
* seed только tie-break;
* per-room report.

---

# 19. Acceptance gates

## Геометрия

```text
layout_unassigned_interior_ratio_percent <= 3 RECT
layout_unassigned_interior_ratio_percent <= 5 irregular

layout_wall_cleanup_removed_count = 0 RECT
layout_wall_cleanup_spur_count = 0
layout_wall_stub_count = 0
layout_single_tile_notch_count = 0
layout_stair_step_partition_count = 0

layout_corridor_wall_canyon_count = 0
layout_route_dead_end_excess_count = 0
layout_required_adjacency_geometry_missing_count = 0
```

## Композиция

```text
layout_required_module_fallback_count = 0
layout_required_module_reject_count = 0
layout_atomic_module_fragmentation_count = 0
layout_room_composition_missing_count = 0
layout_room_capacity_shortfall_count = 0
layout_scene_underfill_count = 0
layout_underfurnished_room_count = 0
```

## Топология

```text
layout_functional_room_count_gap = 0
layout_actual_topology_signature_count >= 2 standard/spacious
layout_route_component_count = 1
layout_open_bay_without_semantic_owner_count = 0
```

Compact допускает одну hard-valid topology signature.

---

# 20. Визуальный Definition of Done

## Первый приложенный результат

Должен измениться так:

* центральный bay существует только как именованная semantic room;
* bay имеет выраженную composition и aisle pattern;
* периферийные комнаты не образуют равномерное кольцо;
* каждый repeated room имеет полноценную authored composition;
* нет мелких wall teeth и случайных ниш;
* остаток площади не отмечен как circulation автоматически.

## Второй приложенный результат

Должен измениться так:

* размер комнат соответствует их мебели;
* столовая группа не плавает в пустом зале;
* bed group является частью bedroom composition;
* неиспользуемые крупные прямоугольники отсутствуют;
* public, private и service spaces различаются формой и transition policy;
* corridor является маршрутом, а не побочным эффектом промежутков между комнатами.

---

# 21. Команды проверки

```bash
git diff --check

tools/build/build --ci dm \
  -DUNIT_TESTS \
  -DCIBUILDING \
  -DANSICOLORS \
  -Werror
```

После targeted cases:

```text
living
workshop
storage
kitchen
laboratory
dormitory
hydroponics
```

запустить:

```text
10 seeds
× 4 directions
× compact/standard/spacious
```

Для каждой выборки сохранять:

```text
hard-valid rate
distinct topology signatures
mean residual ratio
mean route ratio
composition failure rate
wall defect rate
```

---

## Итоговая оценка

Моя уверенность:

* **94%** — текущий главный дефект создают псевдо-family, universal room-to-route и blanket circulation overlay;
* **92%** — required compositions необходимо переводить на полностью atomic authored modules;
* **85%** — equal-area room sizing существенно отвечает за огромные пустые помещения;
* **80%** — предложенные числовые mapping thresholds подходят как стартовые, но требуют калибровки seed matrix.

Основная задача следующего прохода — не “добавить ещё правил”, а **удалить логику, которая автоматически превращает плохой candidate в формально валидный**, после чего реализовать настоящие topology, route и composition contracts в текущем canonical solver.
































# Итоговый вердикт

Текущий head PR #99 — `91afe9c71c4a5e156c73d4972caac292e4745394`. PR остаётся draft; CI для этого SHA на момент проверки только поставлен в очередь, результата сборки и тестов ещё нет.

**Коммит существенно продвинул архитектурный каркас**, но работу нельзя считать завершённой. Сейчас реализованы почти все заявленные сущности — topology graph, family policies, bounded beam allocation, terminal A*, ownership partitions, atomic compositions, quality vector. Однако между названиями этих компонентов и их фактическими гарантиями остаётся разрыв.

Моя оценка:

| Область                           |      Готовность |
| --------------------------------- | --------------: |
| Контракты и структуры данных      |             75% |
| Компиляция semantic graph         |             60% |
| Реальные topology families        |             35% |
| Graph-aware allocation            |             45% |
| Route network                     |             45% |
| Partition/wall materialization    |             35% |
| Atomic composition                |             55% |
| Quality gates                     |             55% |
| Reporting/acceptance              |             35% |
| Удаление старого кода             |             10% |
| Подтверждённая runtime-готовность | не подтверждена |

**Предпочтительный путь — 90%:** сохранить полезные новые datums и bounded infrastructure, но переработать внутренние алгоритмы в текущем canonical solver.

**Альтернатива — 10%:** откатить весь последний коммит и реализовать заново от `cc210...`. Не рекомендую: будут потеряны полезные контракты, A*, beam scaffold, composition contract и улучшенная диагностика.

Свежие `semantic.png`, `semantic.json` и `report.json` для нового SHA в этом сообщении не приложены, поэтому визуальное качество именно `91afe...` пока не подтверждено. Анализ ниже основан на актуальном коде head.

---

# 1. Что в коммите сделано правильно

## 1.1 Один canonical pipeline сохранён

Новые компоненты подключены внутри существующего `building_layout`, без `v3`, runtime-флага или второго generator path:

```text
solver_topology
solver_allocator
solver_route
solver_partitions
solver_openings
solver_composition
solver_quality
solver
```

Это правильная организационная основа.

## 1.2 Functional rooms отделены от circulation

Program contract теперь хранит:

```dm
functional_room_contracts
circulation_contracts
target_functional_room_count
composition_contracts
```

`target_room_count` компилируется по functional spaces, а `entry`, `route` и `choke` больше не обязаны засчитываться как полноценные помещения. Это исправляет один из основных дефектов предыдущего состояния.

## 1.3 Disconnected required graph больше не ремонтируется автоматически

Топология теперь включает functional, circulation и transition nodes. Required nodes, недостижимые от root, формируют compile errors вместо скрытого подключения к root.

## 1.4 Появился bounded beam allocator

Добавлены явные bounds:

```text
beam width = 6
до 8 rectangle candidates на node
до 96 partial expansions
```

Это лучше старого последовательного greedy placement и создаёт основу для реального constraint allocation.

## 1.5 Route теперь действительно ищется bounded A*

Маршрут строится после размещения помещений, использует terminals, path costs, turn penalties и branch pruning. Это принципиально лучше прежнего seed-only route.

## 1.6 Required composition groups размещаются до optional

Новый composition solver:

* резервирует negative space до мебели;
* проходит все `required_groups`;
* отклоняет required group при невозможности размещения;
* откатывает optional group целиком;
* проверяет contiguous wall axis.

Это правильное направление.

## 1.7 Candidate selection стал значительно строже

Теперь кандидаты:

* различаются по topology signature;
* получают quality vector;
* сравниваются лексикографически;
* допускают seed только среди практически равных кандидатов с одинаковым quality prefix и разницей до 0,5%.

Это лучше прежнего случайного выбора из широкого 10%-го диапазона.

---

# 2. Блокирующие проблемы текущего состояния

## P0.1. Family policies пока остаются преимущественно геометрическими пресетами

Формально объявлены семь families, но `hard_validate()` базовой policy сейчас проверяет только:

```dm
candidate.topology_family == id
length(candidate.room_plans) > 0
```

Он не валидирует:

* количество spokes;
* разделение крыльев;
* secure threshold;
* nested containment;
* open-bay ownership;
* courtyard;
* max branch depth;
* route policy;
* family ratios.

`hub_spoke`, `split_wing`, `open_bay_perimeter`, `secure_core`, `nested_service` и остальные сейчас в основном задают набор прямоугольных seed regions. Это уже лучше одного универсального поля, но **название family пока не является архитектурной гарантией**.

### Что требуется

Для каждого family создать свой override:

```dm
/datum/world_edit_building_layout_family_policy/hub_spoke/hard_validate(...)
```

с проверками конкретных инвариантов, а не только family id.

---

## P0.2. `route_hints` создаются, но canonical route solver ими не управляется

Family policies добавляют:

```dm
hub_cross
wing_axis
bay_aisle
secure_approach
service_threshold
courtyard
```

Но terminal route solver строит сеть от entry к room terminals через A*; family route hints в просмотренном canonical route path не участвуют. Это означает, что `courtyard`, `hub_cross` и `wing_axis` пока являются декларативными метками без гарантированной геометрии.

### Что требуется

Заменить `route_hint` на typed route constraints:

```dm
/datum/world_edit_building_layout_route_constraint
    var/kind
    var/list/terminal_ids
    var/list/preferred_turfs
    var/list/required_turfs
    var/min_width
    var/max_width
    var/owner_room_id
```

A* должен учитывать их как обязательные или предпочтительные сегменты.

---

## P0.3. Circulation nodes снова схлопываются в строку `"route"`

В topology graph различаются:

```text
FUNCTIONAL
CIRCULATION_TERMINAL
TRANSITION
```

Но при создании candidate connections любой non-functional endpoint преобразуется в `"route"`.

Это уничтожает различие между:

```text
entry_buffer
central_aisle
clean_spine
serving_edge
service_aisle
secure_choke
```

Например две разные связи:

```text
entry_buffer ↔ dining
serving_edge ↔ dining
```

превращаются в две одинаковые:

```text
route ↔ dining
```

Валидатор также нормализует все circulation endpoints в `"route"`.

### Последствия

* возможны дублирующиеся opening connections;
* circulation graph теряет порядок переходов;
* нельзя гарантировать `entry → public → choke → secure`;
* route ownership восстанавливается постфактум по эвристикам;
* topology signature не отражает реальную circulation semantics.

### Что требуется

Сохранить отдельные IDs до финальной route materialization:

```text
route:entry_buffer
route:central_aisle
route:serving_edge
route:secure_choke
```

`"route"` должен быть только типом области, но не идентификатором всех circulation nodes.

---

## P0.4. Тип opening может быть перезаписан room policy

Region builder выводит opening kind из topology edge:

```text
SECURE → secure door
OPEN_MERGE → wide arch
NESTED → door
```

Но `get_building_layout_connection_opening_kind()` сначала берёт `room_contract.route_opening_kind`, и только при его отсутствии — `connection.kind`. Поэтому semantic edge может быть проигнорирован.

Например:

```text
public room → secure room
```

может получить public arch, если `from_room` оказался public и его contract был выбран первым.

### Что требуется

Разделить:

```dm
connection.edge_kind
connection.opening_policy
connection.route_policy
```

Приоритет:

```text
explicit edge opening policy
→ privacy transition
→ room default
→ generic fallback
```

Никаких перегруженных строк `"shared"`, `"route"`, `"door"` в одном `kind`.

---

## P0.5. Allocator пока не реализует разные edge semantics

Beam allocator существует, но non-route edges в основном проверяются одинаково: через наличие некоторого partition overlap. Он не различает достаточным образом:

* `SHARED`;

* `OPEN_MERGE`;

* `SECURE`;

* `NESTED`.

Одновременно conflict check резервирует вокруг каждого размещённого помещения одноклеточное кольцо. Это делает настоящее `NESTED`-размещение практически невозможным: child не может находиться внутри parent, потому что любые пересечения запрещаются.

### Что требуется

Для каждого edge отдельный candidate generator:

```text
SHARED
    rectangles разделены wall band 1 tile;
    shared wall run >= min_shared_wall.

OPEN_MERGE
    непосредственное касание floor regions;
    merge segment 2–4 tiles;
    partition отсутствует.

SECURE
    wall band 1 tile;
    controlled opening segment;
    protected approach.

NESTED
    child bounds внутри parent bounds;
    margin >= declared margin;
    отдельный ownership island;
    один controlled opening.

ROUTE
    room имеет access port;
    прямое shared adjacency не требуется.
```

---

## P0.6. Beam выбирает первый complete candidate, а не лучший complete candidate

После beam expansion allocator перебирает завершённые partials и возвращает первый, для которого `route_network_possible() == TRUE`. Он не сравнивает полную route cost, residual geometry и partition quality всех завершённых partials.

### Что требуется

Для top complete partials выполнять cheap dry route:

```text
estimated_route_cost
route_turn_count
dead_end_count
residual_component_count
partition_defect_estimate
family_constraint_score
```

Выбирать лучший route-feasible partial, а не первый.

---

## P0.7. Route solver намеренно начинает с самого дальнего terminal

При создании основной ветви:

```dm
best_cost = -1

if(indexed_cost > best_cost)
    first_terminal = indexed
```

То есть первым выбирается terminal с **максимальной**, а не минимальной стоимостью.

Это создаёт длинный основной ствол через здание, после которого остальные комнаты подключаются боковыми ветвями. По сути это воспроизводит прежний axial bias уже внутри A*.

### Что требуется

Предпочтительный вариант — **82%**:

```text
строить MST-подобную сеть:
каждый раз подключать terminal с минимальной incremental cost к существующей сети
```

Альтернатива — **18%**:

```text
сначала соединить наиболее дальний terminal как trunk
```

Она допустима только для явно axial/corridor-oriented family и не должна быть общим поведением.

---

## P0.8. Route ownership назначается нестабильно

После построения route zone ownership:

* связывается с room reservations;
* затем первые `entry_contract.min_area` элементов `candidate.route_turfs` переписываются как entry zone.

Но `route_turfs` — порядок вставки paths, а не гарантированный BFS-порядок от входа.

### Что требуется

Сегментировать route graph от entry:

```text
BFS distance 0..N → entry_buffer
transition segment → choke
branches → service/clean aisle
```

Circulation ownership должен назначаться path segments, а не первыми элементами списка.

---

## P0.9. Partition graph пока остаётся room-perimeter rasterizer

Текущий partition pass:

1. проходит все room turfs;
2. смотрит клетку через предполагаемую стену;
3. добавляет wall tile;
4. затем отдельным проходом добавляет diagonal corner joins.

Это всё ещё:

```text
обвести помещение стенами
→ заполнить углы
```

а не:

```text
получить boundary segments между ownership regions
→ классифицировать segment
→ материализовать его один раз
```

### Особо опасный код

```dm
materialize_building_layout_partition_corner_joins()
```

Добавляет диагональную клетку стены, если рядом существуют две ортогональные. Это прямой источник:

* утолщённых углов;
* wall teeth;
* одноклеточных выступов;
* ложных notch;
* спорных стыков.

### Что требуется

Полностью удалить `materialize_building_layout_partition_corner_joins()`.

Стены строить через canonical edge segments:

```dm
/datum/world_edit_building_partition_segment
    var/owner_a
    var/owner_b
    var/orientation
    var/list/turfs
    var/kind
    var/opening_policy
```

---

## P0.10. `open_bay_perimeter` может насильно сделать root открытым

Partition code считает room открытым bay, если выполняется хотя бы одно:

```text
seed zone role == open_bay
room.spatial_kind == OPEN_BAY
family == open_bay_perimeter && room == root
```

Последнее условие нарушает semantic contract: root может быть secure, storage или service room, но family всё равно заставит его вести себя как open bay.

### Что требуется

Family `open_bay_perimeter` доступна только если topology graph содержит room contract:

```dm
spatial_kind == OPEN_BAY
```

Именно эта room становится bay owner. Никакого override по `root_node_id`.

---

## P0.11. Unassigned interior validator сейчас фактически сломан

После удаления blanket overlay residual клетки больше не получают label:

```text
circulation_open_bay
```

Но validator считает unassigned только так:

```dm
if(candidate.route_zone_by_turf[interior_turf] == "circulation_open_bay")
    unassigned_lookup[interior_turf] = TRUE
```

Следовательно, большой остаток, не принадлежащий ни room, ни route, может снова давать:

```text
layout_unassigned_interior_excess_count = 0
layout_ownerless_open_bay_count = 0
```

Это новый false-green.

### Правильный расчёт

```dm
unassigned =
    interior
    - room_floor_lookup
    - route_lookup
    - wall_lookup
    - opening_lookup
    - explicit_structural_buffer_lookup
```

Нельзя определять residual по старому служебному label.

---

## P0.12. Room sizing всё ещё делит площадь почти поровну

Текущий room compiler:

```text
useful_room_budget
÷ target_room_count
→ average_room_area
→ preferred_area
```

Это сохраняет риск чрезмерно крупных комнат:

* маленькая utility room получает долю большой площади;
* storage растягивается без увеличения capacity;
* room scene остаётся минимальной, а площадь растёт;
* open bay не становится единственным осмысленным поглотителем surplus.

### Что требуется

Room area должна исходить из authored module footprint:

```text
required occupied cells
+ interaction clearance
+ door-to-focus lane
+ wall/service frontage
+ role-specific slack
```

Surplus распределять:

1. в явно declared open bay;
2. в public hub до max ratio;
3. в structural buffers;
4. иначе candidate отклонять как overlarge footprint/program mismatch.

---

## P0.13. Composition solver всё ещё использует count credit вместо полной семантики

Required group считается удовлетворённым, если сумма `get_building_fixture_count_credit()` достигла `min_count`.

Это не проверяет, что:

* table и chairs принадлежат одному module instance;
* storage aisle имеет проход;
* beds имеют фронтальный доступ;
* kitchen cooking line содержит разные функциональные slots;
* lab group не состоит из одинаковых заменителей;
* hydro trays образуют row/grid.

### Что требуется

Composition group должен проверять module instance:

```dm
required_module_recipe_ids
required_slot_multiset
required_capacity
required_relative_relations
required_clearance_mask
```

Не количество абстрактных credits.

---

## P0.14. Required group всё ещё может уйти в generic fallback placement

Composition solver вызывает `add_building_layout_cluster_module()`.

Если curated modules существуют, используется atomic path. Если curated module не найден или список пуст, остаются generated/generic paths, включая одиночный anchor placement или special table fallback.

Catalog содержит curated recipes для большого числа clusters, но также регистрирует generated modules для всех furniture clusters.

### Правило

```text
required group:
    curated authored module only
    explicit compact curated substitute
    иначе compile/preflight failure

optional detail:
    generated module допустим
```

Добавить:

```text
layout_required_group_without_curated_module_count
layout_generated_module_used_for_required_group_count
```

Оба должны быть `0`.

---

## P0.15. Wall-module validation использует `dir`, а не явный `wall_dir`

`building_layout_wall_group_has_contiguous_axis()` сравнивает `member["dir"]`. Но direction объекта и направление стены — не всегда одно и то же, особенно для place rules с `face_away`, `parallel`, custom orientation.

### Что требуется

Каждый member plan должен хранить:

```dm
wall_dir
front_dir
interaction_dir
```

Wall-axis validation должна использовать `wall_dir`, а не object `dir`.

---

## P0.16. Legacy masking code всё ещё физически присутствует

В canonical `room_allocator.dm` остаются старые определения:

```text
materialize_building_layout_circulation_overlay
solve_building_layout_route_after_rooms
connect_building_layout_route_to_rooms
building_layout_room_plan_has_route_partition
```

Хотя новый entrypoint уже вызывает bounded allocator и terminal route.

В `solver_programs.dm` остаются:

```text
build_building_layout_scene_instance_support_spec
reserve_building_layout_required_scene_identity_budget
configure_building_layout_scene_fallback
```

Старые semantic solver/model/rules/emitter/validation, room graph и BSP всё ещё подключены в `_world_edit.dme`.

Это опасно по двум причинам:

1. трудно доказать, что masking path действительно недостижим;
2. новые изменения могут случайно снова вызвать старую функцию.

---

## P0.17. Два canyon validator продолжают жить параллельно

Архитектурный validation всё ещё вызывает старый immediate-neighbor detector, который плохо видит широкие route bands.

Позже review contract вызывает новый route-band detector.

Один counter вычисляется двумя разными способами в разные моменты pipeline. Это создаёт order-dependent результат.

### Что требуется

Удалить старый detector полностью. Оставить только route-band implementation.

---

## P0.18. Topology diversity может быть искусственно завышена

Topology signature содержит:

* абсолютные bounds каждой комнаты;
* parent id;
* каждую route tile с world coordinates.

Поэтому два геометрически почти одинаковых layout с route, сдвинутым на одну клетку, считаются разными topology signatures.

### Нужно разделить

```text
structural_topology_signature
    graph degree sequence
    edge kinds
    depth distribution
    room side/wing assignment
    open-bay/nested counts
    route branch degree sequence

geometry_layout_hash
    точные room bounds
    route tiles
    wall tiles
```

Diversity gate должен использовать structural signature.

---

# 3. Текущее состояние тестов и acceptance

Task-state самого коммита всё ещё отмечает M4–M12 как `PENDING`, а 840-case matrix, all-program smoke и final audits не выполнены.

При этом кодовая поверхность M4–M10 уже частично добавлена. Значит task-state не синхронизирован с реализацией и не может служить доказательством завершения.

CI для head пока `queued`, поэтому сейчас нельзя утверждать даже прохождение Werror build на GitHub.

Living visual case уже содержит новые hard expectations, но этого недостаточно: аналогичные строгие role-specific contracts должны быть добавлены в workshop, kitchen, laboratory, dormitory, hydroponics, storage и остальные программы.

---

# 4. Целевая архитектура текущего solver

Новый entrypoint создавать не нужно. Целевой pipeline:

```text
Semantic archetype
→ Typed program contract
→ Typed topology graph including circulation nodes
→ Family policy with hard invariants
→ Graph-constrained bounded beam allocation
→ Explicit access ports
→ Family-aware terminal route network
→ Ownership regions and owner-bound aisles
→ Canonical partition segments
→ Openings derived from edge policy
→ Final walls without cleanup
→ Authored atomic composition modules
→ Trial emission and hard review contract
→ Lexicographic selection
→ Existing emitter/apply/undo
```

---

# 5. Детальный план дальнейшей работы

## Этап 0. Зафиксировать честный baseline для `91afe...`

До следующего изменения нужны свежие artifacts с точным SHA:

```text
building_living_target_rooms_6
building_hydroponics_target_rooms_7
building_workshop_target_rooms_6
building_kitchen_representative
building_laboratory_representative
building_dormitory_target_rooms_7
building_storage_target_rooms_5
```

Сохранить:

```text
report.json
semantic.json
semantic.png
semantic_sprites.png
candidate rejection reports
```

### Exit criteria

* SHA указан в report;
* CI завершён;
* expectation diff сохранён;
* нельзя менять expectations, пока generator не исправлен.

---

## Этап 1. Удалить мёртвые и маскирующие paths

### Файлы

```text
solver/building_layout_solver_room_allocator.dm
solver/building_layout_solver_programs.dm
solver/building_layout_solver_quality.dm
modular/world_edit/_world_edit.dme
semantic/*
building_layout_room_graph.dm
building_layout_bsp.dm
```

### Удалить после zero-callsite audit

```text
materialize_building_layout_circulation_overlay
solve_building_layout_route_after_rooms
connect_building_layout_route_to_rooms
building_layout_room_plan_has_route_partition

build_building_layout_scene_instance_support_spec
reserve_building_layout_required_scene_identity_budget
get_building_layout_scene_identity_detail_category
required singleton fallback

старый immediate-neighbor canyon detector
старый semantic solver/emitter
legacy BSP/room graph/divider datums
```

### Обязательные команды discovery

```bash
rg -n \
  "materialize_building_layout_circulation_overlay|\
solve_building_layout_route_after_rooms|\
connect_building_layout_route_to_rooms|\
building_layout_room_plan_has_route_partition" \
  modular/world_edit/code/generators/building_layout

rg -n \
  "build_building_layout_scene_instance_support_spec|\
reserve_building_layout_required_scene_identity_budget|\
configure_building_layout_scene_fallback" \
  modular/world_edit/code/generators/building_layout

rg -n \
  "world_edit_building_semantic_|\
world_edit_building_room_graph|\
world_edit_building_bsp|\
world_edit_building_divider" \
  modular/world_edit/code/generators/building_layout
```

### Exit criteria

* определения удалены;
* callsites = 0;
* includes удалены;
* Werror build проходит;
* никакой compatibility wrapper не добавлен.

---

## Этап 2. Нормализовать topology contracts

### Изменить connection datum

```dm
/datum/world_edit_building_layout_connection_contract
    var/from_node_id
    var/to_node_id

    var/edge_kind
    var/opening_policy
    var/route_policy

    var/required
    var/min_shared_wall
    var/min_opening_width
    var/max_opening_width
```

### Не нормализовать circulation в `"route"`

Сохранять:

```text
entry_buffer
central_aisle
serving_edge
clean_spine
secure_choke
```

до route graph materialization.

### Exit criteria

* circulation-to-circulation edges сохраняют IDs;
* secure/nested/open edges имеют явную opening policy;
* required topology geometry validator не использует строковую нормализацию `"route"`.

---

## Этап 3. Реализовать hard constraints для каждой family

### `hub_spoke`

```text
root functional/open bay существует;
2–4 root children;
дети занимают минимум две стороны root;
max depth <= 2 или family contract;
нет длинного общего коридора.
```

### `split_wing`

```text
два непустых wings;
root/transition между ними;
private/secure groups не смешиваются с public wing;
route между wings не проходит через functional room.
```

### `open_bay_perimeter`

```text
ровно один explicit OPEN_BAY owner;
bay ratio 35–60%;
service rooms на периметре;
owner-bound aisle существует;
никакой root override.
```

### `secure_core`

```text
secure core не граничит со входом;
существует один controlled threshold;
public path не входит в core без threshold.
```

### `nested_service`

```text
nested child действительно contained;
margin соблюдён;
один controlled opening;
нет отдельной corridor branch к nested child.
```

### `compound_cells`

```text
courtyard имеет owner;
минимум два pods;
pods не образуют одну общую прямоугольную комнату;
courtyard связан с entry.
```

### `axial_fallback`

```text
только compact / min side <=11 / aspect >=1.7;
выбирается только при отсутствии non-axial hard-valid candidate.
```

### Exit criteria

У каждой family есть unit test:

```text
valid candidate accepted
candidate with broken defining invariant rejected
```

---

## Этап 4. Доработать beam allocator

### Partial state должен хранить

```dm
placements
satisfied_edges
pending_edges
reserved_partition_turfs
reserved_opening_segments
estimated_route_terminals
residual_components
family_group_occupancy
score
```

### Edge-specific rectangle generators

```text
build_shared_rect_candidates()
build_open_merge_rect_candidates()
build_nested_rect_candidates()
build_secure_rect_candidates()
build_route_terminal_rect_candidates()
```

### Изменить выбор результата

Не возвращать первый complete partial.

Для top 4–6 complete partials:

```text
cheap route solve
residual calculation
partition estimate
family hard validate
```

Затем выбрать лучший.

### Exit criteria

* nested room действительно находится внутри parent;
* OPEN_MERGE не создаёт wall band;
* SHARED имеет общий segment;
* SECURE имеет protected threshold;
* residual считается до route emission.

---

## Этап 5. Исправить terminal route network

### Главная правка

Заменить выбор наиболее дальнего first terminal на минимальную incremental cost:

```dm
next_terminal = argmin(
    cost(route_network → terminal)
)
```

Исключение допускается только для `axial_fallback` с явной trunk policy.

### Route policy должна учитывать family

```text
hub_spoke        → короткие radial branches
split_wing       → trunk между wing junctions
open_bay         → owner aisle внутри bay
secure_core      → один protected approach
nested_service   → nested child без общего route terminal
compound_cells   → courtyard connector network
```

### Добавить post-processing

```text
remove redundant loops
prune non-terminal dead ends
orthogonal smoothing
staircase collapse
parallel branch elimination
width normalization
```

### Метрики

```text
layout_route_turf_count
layout_route_area_ratio_percent
layout_route_turn_count
layout_route_dead_end_count
layout_route_longest_dead_end
layout_route_branch_count
layout_route_parallel_segment_count
```

---

## Этап 6. Переписать partition materialization

### Удалить diagonal corner join

```dm
materialize_building_layout_partition_corner_joins()
```

### Новый алгоритм

1. Построить ownership lookup.
2. Извлечь границы между ownership regions.
3. Сгруппировать границы в straight segments.
4. Назначить segment kind по topology edge.
5. Вырезать opening slots.
6. Материализовать walls один раз.
7. Проверить mapping defects.
8. Никакого cleanup для RECT.

### Mapping gates

| Правило                   |               Требование |
| ------------------------- | -----------------------: |
| Internal wall thickness   |                   1 tile |
| Wall stub                 |                        0 |
| Single-tile notch         |                        0 |
| Stair-step partition      |                        0 |
| Misaligned join           |                        0 |
| Controlled door shoulders | ≥1 tile с каждой стороны |
| Public arch width         |                      2–3 |
| Secure/normal door width  |                        1 |
| RECT cleanup removed      |                        0 |
| Irregular cleanup ratio   |                      ≤1% |
| Continuous canyon         |                ≤3 slices |
| Unassigned RECT           |                      ≤3% |
| Unassigned irregular      |                      ≤5% |

---

## Этап 7. Исправить residual ownership

Вычислять:

```dm
assigned_lookup =
    all room floors
    + explicit route floors
    + owner-bound aisles
    + walls
    + openings
    + explicit structural buffers

unassigned = interior - assigned_lookup
```

Никакого поиска старого label `circulation_open_bay`.

### Exit criteria

```text
layout_unassigned_interior_turf_count честно совпадает с semantic overlay;
ownerless components отображаются на PNG;
open bay всегда имеет functional owner.
```

---

## Этап 8. Module-aware room sizing

Для каждого required composition group вычислять:

```text
module occupied bounds
+ clearance bounds
+ interaction lane
+ door approach
+ wall frontage
+ required negative space
```

### Area policy

```text
enclosed room:
    preferred = authored composition footprint + 15–30% slack
    max = preferred × 1.25

open bay:
    preferred = authored minimum
    может поглощать family-controlled surplus

circulation:
    определяется route network
```

Нельзя вычислять все комнаты как равные доли общей площади.

---

## Этап 9. Сделать required compositions полностью authored

### Compiler gate

Каждый required group обязан иметь curated module:

```dm
if(!catalog.has_curated_module_for_cluster(required_group))
    program compile failure
```

### Запретить для required

```text
generated module
single-anchor fallback
table fallback
synthetic support group
budget inflation
partial member acceptance
```

### Расширить composition contract

```dm
/datum/world_edit_building_layout_composition_contract
    var/list/required_module_groups
    var/list/min_capacity_by_kind
    var/min_module_instances
    var/min_occupied_ratio
    var/max_occupied_ratio
    var/interaction_lane_policy
    var/instance_policy
```

---

## Этап 10. Role-specific composition rules

| Program/space          | Обязательное содержимое                              |
| ---------------------- | ---------------------------------------------------- |
| Living common          | social focal + side/support module при area ≥24      |
| Bedroom                | bed + clear approach; storage при area ≥16           |
| Sanitation             | toilet + sink, кроме explicit compact contract       |
| Workshop bay           | workbench/machine run + support storage              |
| Workshop storage       | authored rack module, минимум 2 members при area ≥12 |
| Storage                | rack rows + clear aisle                              |
| Hydro                  | tray row/grid + owner-bound service aisle            |
| Kitchen dining         | table + минимум 2 chairs одного instance             |
| Kitchen serving        | counter run ≥3                                       |
| Kitchen cooking        | cooking run ≥4 functional members                    |
| Kitchen cold storage   | capacity ≥2                                          |
| Dorm sleep bay         | минимум 2 beds на каждый repeated bay                |
| Dorm locker            | минимум 2 cabinets                                   |
| Laboratory machine bay | authored machine group                               |
| Laboratory analysis    | work surface + console/seat + storage/support        |
| Security               | controlled console + secure storage behind threshold |

---

## Этап 11. Устранить дублирование quality logic

Удалить:

```text
старый immediate-neighbor canyon
старый generic scene hierarchy path
post-placement member deletion
generic empty-room heuristics, дублирующие composition contract
```

Оставить единый review pipeline:

```text
contract validation
topology validation
route validation
partition validation
composition validation
post-emit validation
```

Каждый counter вычисляется ровно в одном месте.

---

## Этап 12. Разделить structural signature и geometry hash

### Structural topology signature

```text
node kinds
edge kinds
degree sequence
depth distribution
wing/group assignment
nested edge count
open-bay count
route branch degree sequence
```

### Geometry hash

```text
room bounds
route turfs
walls
openings
module placements
```

Diversity gate использует structural signature. Determinism использует geometry hash.

---

## Этап 13. Reporting и visualizer

Для каждого room экспортировать:

```json
{
  "id": "sleep_bay_2",
  "spatial_kind": "functional_room",
  "area": 18,
  "topology_parent": "central_aisle",
  "topology_depth": 2,
  "required_edges": ["route:central_aisle"],
  "composition_id": "dorm_sleep_bay",
  "module_instances": ["bed_wall_pair"],
  "capacity": {
    "bed": 2
  },
  "occupied_ratio_percent": 11,
  "clearance_ratio_percent": 22,
  "composition_satisfied": true
}
```

Debug overlay должен показывать:

* ownership;
* circulation node IDs;
* topology edges;
* module bounds;
* clearance masks;
* negative space;
* residual;
* partition segments;
* defect turfs.

---

# 6. Рекомендуемая последовательность коммитов

## Commit 1 — dead-path removal and honest residual

```text
удалить old route/overlay/support/fallback paths
исправить unassigned calculation
оставить cases красными, если topology ещё не удовлетворяет gates
```

## Commit 2 — typed topology and opening policies

```text
circulation IDs сохраняются
edge/opening/route policy разделены
required graph validation
```

## Commit 3 — family hard constraints

```text
семь family validators
normalized region rotation
route constraints
```

## Commit 4 — edge-aware beam allocator

```text
shared/open/nested/secure/route generators
best complete partial selection
```

## Commit 5 — route and circulation ownership

```text
incremental minimum-cost A*
family route policy
segment ownership
owner-bound aisles
smoothing
```

## Commit 6 — partition segments

```text
canonical straight wall segments
opening cuts
no diagonal join
zero RECT cleanup
```

## Commit 7 — authored atomic compositions

```text
required curated only
module-level budget
capacity contracts
per-instance rules
```

## Commit 8 — unified quality/reporting and legacy includes

```text
single validators
structural signature
semantic overlays
remove semantic/BSP/room-graph includes
```

## Commit 9 — acceptance matrix

```text
fixed regressions
840-case matrix
all-program smoke
same-seed replay
final audits
```

---

# 7. Definition of Done

## Structural

```text
layout_functional_room_count_gap = 0
layout_required_adjacency_missing_count = 0
layout_required_adjacency_geometry_missing_count = 0
layout_route_component_count = 1
layout_route_component_error_count = 0
layout_ownerless_open_bay_count = 0
```

## Mapping

```text
layout_unassigned_interior_ratio_percent <= 3 RECT
layout_unassigned_interior_ratio_percent <= 5 irregular

layout_wall_stub_count = 0
layout_wall_notch_count = 0
layout_wall_stair_step_count = 0
layout_wall_misaligned_join_count = 0
layout_wall_cleanup_removed_count = 0 RECT
layout_corridor_wall_canyon_count = 0
```

## Composition

```text
layout_room_composition_missing_count = 0
layout_room_capacity_shortfall_count = 0
layout_scene_underfill_count = 0
layout_underfurnished_room_count = 0
layout_atomic_module_fragmentation_count = 0
layout_required_module_fallback_count = 0
layout_required_template_reject_count = 0
layout_required_group_without_curated_module_count = 0
```

## Candidate selection

```text
layout_hard_valid_candidate_count >= 2 standard/spacious
structural_topology_signature_count >= 2 standard/spacious
selected candidate = lexicographic best
seed changes only exact/near-exact ties
same seed reproduces geometry hash
```

---

# 8. Проверочная матрица

## Fixed regression

```text
living
hydroponics
workshop
storage
kitchen
laboratory
dormitory
```

## Полная основная матрица

```text
7 programs
× 10 seeds
× 4 directions
× 3 size profiles
= 840 cases
```

## Затем

```text
15 programs
× preview/apply/undo smoke
```

## Сборка

```bash
git diff --check

tools/build/build --ci dm \
  -DUNIT_TESTS \
  -DCIBUILDING \
  -DANSICOLORS \
  -Werror
```

После этого — полный unit path и релевантные lint/map checks согласно правилам репозитория.

---

# 9. Итоговый приоритет

| Работа                                               | Приоритет |
| ---------------------------------------------------- | --------: |
| Исправить unassigned/residual false-green            |        P0 |
| Сохранить circulation node identity                  |        P0 |
| Реализовать family hard invariants                   |        P0 |
| Edge-specific allocation, включая real nested        |        P0 |
| Исправить first-terminal route strategy              |        P0 |
| Переписать partition segments и удалить corner joins |        P0 |
| Запретить generic required composition fallback      |        P0 |
| Удалить старые reachable/dead paths                  |        P0 |
| Module-aware room sizing                             |        P1 |
| Structural topology signature                        |        P1 |
| Visual overlays и расширенный reporting              |        P1 |
| Performance tuning после correctness                 |        P2 |

**Главный вывод:** последний коммит создал полезный каркас, но пока не доказал корректность пространства. Следующий проход должен быть не добавлением новых counters и не регулировкой score, а доведением уже введённых abstractions до жёстких геометрических контрактов и удалением старой логики, которая всё ещё способна маскировать нарушения.
